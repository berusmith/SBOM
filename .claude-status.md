# 開發進度與決策記錄

**最後更新**: 2026-04-23  
**開發環境**: 家中 + 公司（雙邊同步）

---

## ✅ 已完成

### P3-1: 日期工具 + 狀態傳遞
- **文件**: `utils/date.js`, `utils/colors.js`, 3個共用元件
- **改動**: 10+ 頁面 (window.confirm → ConfirmModal)
- **Commit**: 多個 P1/P2 項目已分別提交

### P3-2: 圖示庫統一 (lucide-react)
- **文件**: `constants/icons.js` + 9 個頁面
- **改動**: 所有 emoji → lucide-react 圖示
- **Commit**: `f685373`

### P3-3: 表單驗證 + 密碼切換
- **文件**: `PasswordInput.jsx`, `validate.js`
- **改動**: Login, Organizations, Users 3 頁面
- **Commit**: `94433f3`

### P3-4: 首屏性能優化 ✨
- **API 分離**: ReleaseDetail 7 個 API 調用 → 2 階段 (立即 + 延遲 100ms)
- **懶加載元件**: 
  - DependencyGraph → `DependencyGraph.jsx` (107 行, lazy import)
  - TrendChart → `TrendChart.jsx` (95 行, lazy import)
- **分頁**: Help.jsx 24 篇文章 → 5篇/頁 + 分頁導航
- **按鈕互動**: Releases 趨勢圖點擊「顯示」才載入
- **編譯**: ✅ 無誤，DependencyGraph/TrendChart 獨立 chunk
- **Commit**: `65f74b4`

---

### P3-5: 無障礙改進 ✨
- **顏色對比**: text-gray-400 → text-gray-600 (138 處)
- **表格無障礙**: 4 個表格 + caption + scope="col"
  - Releases (版本表)
  - ReleaseDetail (元件表 + 漏洞表)
  - Users (用戶表)
- **ARIA 標籤**: PasswordInput Eye/EyeOff 按鈕
- **編譯**: ✅ 無誤 (15.60s)
- **Commit**: `4c7a561`

---

### Phase 2.4a: 韌體掃描基礎 ✨
- **後端模型**: FirmwareScan ORM (UUID, 狀態、進度、元件計數、EMBA JSON 輸出)
- **API 端點**: 
  - POST /api/firmware/upload (多檔案接收 + 背景掃描)
  - GET /api/firmware/scans (掃描清單)
  - GET /api/firmware/scans/{id} (掃描詳情 + 元件解析)
- **前端頁面**: FirmwareUpload.jsx (拖曳上傳、進度追蹤、組件展開、自動重整)
- **服務層**: FirmwareService (EMBA 自動偵測 + 演示模式回退)
- **特點**: 背景任務 + 3秒自動輪詢 + Windows 開發支援
- **編譯**: ✅ 無誤 (28.02s)
- **Commit**: `1d0aea6`

---

## 📊 當前狀態

**最後工作日期**: 2026-04-23  
**完成總進度**: P3-1 ✅, P3-2 ✅, P3-3 ✅, P3-4 ✅, P3-5 ✅, Phase 2.4a ✅  
**Phase 3 完成率**: 100% (5/5 項目)  
**Phase 2.4a 完成率**: 100% (韌體掃描基礎)  
**下一步**: CSV 匯出、TISAX 模組、或其他需求

---

## ⏳ 待做清單 (待選擇)

### 🔧 P3-4: 首屏性能優化
**改動範圍**: 4-5 個檔案 (中等)  
**工作時長**: 2-3 小時

**具體項目**:
1. ReleaseDetail.jsx: 7 個 API 呼叫 → 分離必需 + 懶加載
   - 上傳對比 Diff 結果
   - 漏洞列表
   - Policy Gate / SLA 資訊
   - 依賴圖（SVG 圖表）
   - → 使用 React.lazy + Suspense

2. Help.jsx: 24 篇文章 → 分頁（5篇/頁）
   - 初加載只顯示前 2 個分類
   - 用戶點擊時載入其他分類

3. 圖表懶加載: Releases.jsx 的趨勢圖在第二 Tab 時載入

**預期效果**: ReleaseDetail 首頁 JS 執行時間 < 2 秒

---

### ♿ P3-5: 無障礙改進
**改動範圍**: 10+ 頁面 (廣泛)  
**工作時長**: 3-4 小時

**具體項目**:
1. ARIA 標籤: 所有圖示按鈕加 `aria-label`
   - Eye/EyeOff (密碼切換)
   - 刪除按鈕
   - 摺疊/展開箭頭
   - 搜尋圖示

2. 表格無障礙:
   - 加 `scope="col"` / `scope="row"`
   - 加 `<caption>` 或 `aria-label`

3. 鍵盤導航:
   - Modal 按 ESC 關閉 (已有)
   - Tab 順序邏輯檢查
   - 表單 Enter 提交 (已有)

4. 顏色對比檢查:
   - 灰色文字 (text-gray-500) vs 背景
   - 紅色錯誤訊息 vs 白色背景
   - WCAG AA 標準 (4.5:1)

**預期效果**: 通過 Lighthouse 無障礙評分 90+


---

## 同步說明

此檔案在 GitHub 上，兩邊開發時：
1. 開始前：`git pull` 最新狀態
2. 開發中：紀錄進度（若中途改變計畫）
3. 完成後：`git commit + push` 更新狀態
