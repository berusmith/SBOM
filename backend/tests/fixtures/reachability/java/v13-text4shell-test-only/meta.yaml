fixture_type: cve_reachability
cve_id: CVE-2022-42889
ghsa_id: GHSA-599f-7c49-w659
package: org.apache.commons:commons-text
import_names: [org.apache.commons.text]
ecosystem: maven
affected_versions: ">=1.5,<1.10.0"
language: java
expected_reachable: false
expected_label: test_only
transitive_only: false
vulnerable_symbols:
  - org.apache.commons.text.StringSubstitutor.replace
notes: |
  Production code (`src/main/java/.../HtmlEscaper.java`) uses only
  `StringEscapeUtils.escapeHtml4` — safe.  The test suite
  (`src/test/java/.../TemplateTest.java`) exercises the prefix
  evaluator with `StringSubstitutor.replace(...)` to verify the
  consumer code's input sanitisation defences.

  Java path-aware analyzer must classify the StringSubstitutor
  call as test_only — Maven's `src/test/java/` convention should
  be the same signal as Python's `tests/` segment.
